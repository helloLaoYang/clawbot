# Third-party notices

This project interoperates with the HTTP protocol documented and implemented by Tencent's
[`openclaw-weixin`](https://github.com/Tencent/openclaw-weixin) project. No source code from that
project is vendored here.

Runtime dependencies and their license metadata are recorded in `package-lock.json` and each
installed package's `package.json`.
